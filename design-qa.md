# Design QA

- 参考稿：`/Volumes/res/0804/首页+搜索0727/搜索弹出框/搜索弹出框2.jpg`、`/Volumes/res/0804/首页+搜索0727/搜索弹出框/搜索弹出框3.jpg`
- 本轮局部参考：`/var/folders/_z/0zf4rjl956z5wtj8_t359x3h0000gp/T/codex-clipboard-bf121022-484e-46df-80a2-8f213da19d1f.png`（694 × 210 px）
- 实现页：`http://127.0.0.1:4200/search?q=公告`
- 本轮实现截图：`/tmp/ibomy-history-edit-focused.png`（355 × 67 px，CSS 视口宽 389 px）
- 搜索发现参考：`/Volumes/res/0804/首页+搜索0727/搜索弹出框/搜索弹出框2.jpg`、`搜索弹出框3.jpg`（显示态、隐藏态）
- 搜索发现实现截图：`/tmp/ibomy-search-discovery-full.png`（1280 × 720 px，桌面 CSS 视口 1280 px）
- 复核范围：搜索结果导航、列表容器、卡片分隔、底部操作区、搜索历史普通态与编辑态。

## 结果

通过。搜索结果页已移除独立黑色外框，导航使用首页的粉色底、黄色底线和斜切标签；结果卡片使用首页信息流的白底、粉色间距、头像头部、粗细组合分割线及灰色底部操作区。搜索历史普通态显示垃圾桶与折叠按钮，编辑态显示“全部删除 / 完成”及每条记录的单独删除按钮。

## 浏览器验证

- 搜索结果卡片：4 条正常渲染。
- 搜索结果容器边框：`0px`。
- 卡片边框：`0px`。
- 分割线高度：`1px`，左侧叠加首页同款粗线。
- 分享按钮右侧外边距：`0px`。
- 搜索历史：3 条记录；编辑态出现 3 个单条删除按钮，并显示“全部删除”和“完成”。
- 编辑态局部对比：删除区以绝对定位覆盖胶囊右侧，浅灰背景宽 30px，叉号 13 × 13px；胶囊外框连续。焦点区域足以判断本轮图标、背景、间距和圆角，无需额外全页对比。
- 悬停状态：编辑态胶囊保持白色，仅右侧删除区从 `#f1f1f1` 变为 `#e6e6e6`。
- 搜索发现：4 条后台默认配置按两列显示；长标题单行省略。显示图标、隐藏图标均加载用户提供的 SVG，隐藏后显示“已隐藏”，再次点击可恢复。
- 字体、色彩、边距、胶囊圆角和分割线均沿用搜索弹层现有设计令牌；参考稿为移动端、实现证据为桌面端，因此只比较组件结构与视觉比例，不把响应式缩放差异列为问题。
- 图片资产：使用用户提供的两个原始眼睛 SVG，没有使用替代图形或公共图标库近似。
- 文案与链接：标题、4 条默认发现词及链接均正确渲染；浏览器读取到的 `data-href` 与后台默认配置一致。
- 控制台错误：无。

## 对比记录

- P2：删除叉号原先与文字并排，没有设计稿中的右侧浅灰悬浮区域。已改为绝对定位覆盖右侧，并缩小图标；复核截图中胶囊轮廓、浅灰删除区和叉号均正常。
- P3：鼠标悬停原先会使整个胶囊变色。已锁定编辑态胶囊白色，仅保留删除区反馈。
- P2：缺少设计稿中的“搜索发现”板块及显示/隐藏状态。已增加后台对象列表配置、两列胶囊布局、状态持久化与两个真实 SVG 图标；浏览器复核显示态 4 条、隐藏态 0 条并出现“已隐藏”，恢复后再次显示 4 条。

final result: passed
